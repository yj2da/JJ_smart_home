# 📋 김진아와 오예진의 스마트홈 웹 대시보드 제품 요구사항 정의서 (PRD)

본 문서는 `JJ_smartHome.py` MicroPython 펌웨어를 기반으로 작동하는 **김진아와 오예진의 스마트홈 웹 대시보드**에 대한 제품 요구사항 정의서(PRD)입니다. Web Bluetooth(Nordic UART Service, NUS)를 통해 ESP32 기기와 브라우저 간의 실시간 양방향 제어 및 센서 데이터 모니터링을 구현합니다.

---

## 📌 0. 프로젝트 기본 정보

| 항목 | 내용 |
| :--- | :--- |
| **프로젝트명** | JINJIN 스마트홈 웹 대시보드 (Jina's Smart Home Dashboard) |
| **제작자** | 김진아 (Jina), 오예진(Yejin) |
| **BLE 기기명** | `ESP_JJ` |
| **OLED 이미지 캐릭터** | `img/kitty.pbm` (헬로키티 단색 비트맵 이미지) |
| **디자인 테마** | **Midnight Slate (미드나잇 슬레이트 - 모던 다크 & 글래스모피즘)** - 시크하고 세련된 차콜 & 일렉트릭 스카이 블루 톤 |
| **핵심 차별화 기능** | **오늘 하루 모드 제어(수면/기상/집중), 일정 및 날씨 조회, 수면 질 그래프, 홈 제어 및 간편 AI 챗봇** |

---

## 🔩 1. 하드웨어 시스템 사양 및 핀 매핑 (Hardware & Pin Map)

ESP32 DEVKIT V1 (30핀) 마이크로컨트롤러와 센서/액추에이터 간의 하드웨어 연결 정의입니다.

| 하드웨어 컴포넌트 | 모델 / 사양 | ESP32 GPIO 핀 | 입출력 형태 | 주요 기능 |
| :--- | :--- | :--- | :--- | :--- |
| **터치 센서 1번** | TTP223 | **D17** (GPIO17) | 디지털 입력 | 코골이 감지 모드 시작 (`mic_active = True`) |
| **터치 센서 2번** | TTP223 | **D5** (GPIO5) | 디지털 입력 | 코골이 감지 모드 종료 (`mic_active = False`) / LED 켜기 |
| **터치 센서 3번** | TTP223 | **D18** (GPIO18) | 디지털 입력 | 날씨/온습도 강제 업데이트 및 LED 켜기 |
| **터치 센서 4번** | TTP223 | **D19** (GPIO19) | 디지털 입력 | 피에조 부저 8음계 스케일 테스트 |
| **RGB LED (빨강)** | 공통 캐소드 RGB | **D25** (GPIO25) | 디지털 출력 | 상태 지시 및 조명 제어 |
| **RGB LED (초록)** | 공통 캐소드 RGB | **D26** (GPIO26) | 디지털 출력 | 상태 지시 및 조명 제어 |
| **RGB LED (파랑)** | 공통 캐소드 RGB | **D27** (GPIO27) | 디지털 출력 | 상태 지시 및 조명 제어 |
| **조도 센서 (CdS)** | GL5516 광도전 셀 | **D36** (GPIO36/VP) | 아날로그 입력 (ADC) | 실시간 밝기 감지 (임계값 4000 기준 블라인드 제어) |
| **사운드 마이크 센서** | 고감도 마이크 모듈 | **D34** (GPIO34) | 아날로그 입력 (ADC) | 코골이 소리 레벨 측정 및 호흡 패턴 수집 |
| **서보 모터** | SG90 | **D13** (GPIO13) | PWM 제어 출력 | 스마트 창문 블라인드 제어 (베개 조절 기능 제거) |
| **온습도 센서** | DHT11 | **D14** (GPIO14) | 1-Wire 디지털 | 실시간 실내 온도 및 습도 측정 |
| **피에조 부저** | 패시브 부저 | **D23** (GPIO23) | PWM 주파수 출력 | 알림음, 멜로디 재생 (학교종, 아기상어, 알람 경보음) |
| **OLED 디스플레이 1** | SSD1306 (128x64) | **SDA: D21, SCL: D22** | SoftI2C | OpenWeatherMap API 연동 날씨 정보 및 그래픽 아이콘 표시 |
| **OLED 디스플레이 2** | SSD1306 (128x64) | **SDA: D4, SCL: D16** | SoftI2C | 실내 온습도 수치 및 Kitty 비트맵 이미지(`img/kitty.pbm`) 표시 |

---

## 📡 2. 블루투스 BLE NUS 통신 규격 (BLE Specification)

* **서비스 UUID**: `6e400001-b5a3-f393-e0a9-e50e24dcca9e`
* **RX 특성 UUID (웹 → ESP32)**: `6e400002-b5a3-f393-e0a9-e50e24dcca9e` (Write)
* **TX 특성 UUID (ESP32 → 웹)**: `6e400003-b5a3-f393-e0a9-e50e24dcca9e` (Notify)
* **연결 대상 기기명**: `ESP_JJ`

---

## 🎮 3. 데이터 송수신 및 명령어 프로토콜 (Command Protocol)

### 3.1. 웹 대시보드 → ESP32 제어 명령 (Web to Device)

| 명령어 문자 (ASCII) | 기능명 | ESP32 수행 동작 |
| :---: | :--- | :--- |
| **`'1'`** (또는 `'w'`) | 날씨/온습도 조회 | OpenWeatherMap API 조회 후 OLED 1에 날씨 그래픽 표시, OLED 2에 온습도 표시 및 웹에 데이터 송신 |
| **`'2'`** | 조도 센서 측정 | CdS 센서 ADC 값을 읽어 OLED 1에 밝기 단계 표시 및 웹에 조도 값 송신 |
| **`'3'`** | OLED 1 켜기 | OLED 1 디스플레이 전원 온 (`display1.poweron()`) |
| **`'4'`** | OLED 1 끄기 | OLED 1 디스플레이 전원 오프 (`display1.poweroff()`) |
| **`'5'`** | 멜로디 1 재생 | 피에조 부저로 '학교종' 멜로디 연주 |
| **`'6'`** (또는 `'10'`) | 멜로디 2 재생 | 피에조 부저로 '아기상어' 멜로디 연주 |
| **`'7'`** | RGB LED 전체 켜기 | 빨강/초록/파랑 LED 모두 켬 (흰색 조명) |
| **`'8'`** | RGB LED 전체 끄기 | 빨강/초록/파랑 LED 모두 끔 |
| **`'9'`** | Kitty 이미지 표시 | OLED 2 디스플레이에 `img/kitty.pbm` 비트맵 드로잉 |
| **`'S'`** | 수면 모드 활성화 | 코골이 감지 모드 시작 (`mic_active = True`), RGB LED 빨간색 켬 |
| **`'Q'`** | 수면 모드 비활성화 | 코골이 감지 모드 종료 (`mic_active = False`), RGB LED 끔 |
| **`'A'`** | 알람 비프음 오프 | 피에조 부저 경보음 즉시 종료 (`buzzer.duty_u16(0)`) |
| **`'M<angle>'`** (예: `M90`) | 블라인드 모터 제어 | 서보 모터를 해당 각도로 직접 회전 제어 (`motor.move(angle)`) |

### 3.2. ESP32 → 웹 대시보드 데이터 피드백 (Device to Web)

| 데이터 패킷 예시 | 웹앱 파싱 규칙 (정규식/조건문) | 대시보드 UI 업데이트 항목 |
| :--- | :--- | :--- |
| `temp : 24\n` | `msg.includes('temp')` | 대시보드의 실내 온도 값을 `24°C`로 업데이트 |
| `humi : 55\n` | `msg.includes('humi')` | 대시보드의 실내 습도를 `55%`로 업데이트 |
| `342\n` (숫자 단독) | `/^\d+$/.test(msg)` | 대시보드의 실조도 ADC 값 업데이트 및 조도 그래픽 반영 |
| `Snore Monitor Started\n` | `msg.includes('Snore Monitor Started')` | 수면 모드 활성화 상태 지시등 ON 및 수면 분석 시작 |
| `Snore Monitor OFF\n` | `msg.includes('Snore Monitor OFF')` | 수면 모드 비활성화 상태 지시등 OFF |
| `Mic Level: 45\n` | `msg.includes('Mic Level')` | 실시간 코골이 마이크 음량을 수면 질 차트(그래프)에 점으로 추가 |
| `SNORING_ALERT\n` | `msg.includes('SNORING_ALERT')` | 대시보드 전체에 **"🚨 코골이 감지 경보! 부저 작동 중"** 알림 팝업 노출 및 알람 오프 버튼 활성화 |
| `Piezo Buzzer Test\n` | `msg.includes('Piezo Buzzer Test')` | 피에조 부저 테스트 상태 모션 UI 동작 |

---

## 🎨 4. UI/UX 디자인 요구사항 (Design Guidelines)

### 4.1. 비주얼 테마: **Midnight Slate (미드나잇 슬레이트 - 모던 다크 & 글래스모피즘)**
* **배경색 (Background)**: `#0B0F19` (깊은 다크 네이비 블랙)
* **카드 배경**: `rgba(22, 28, 45, 0.7)` + `backdrop-filter: blur(20px)` (반투명 다크 글래스)
* **테두리 선 (Border)**: `rgba(255, 255, 255, 0.06)`
* **메인 포인트 컬러 (Accent)**: `#38BDF8` (일렉트릭 스카이 블루) 및 `#818CF8` (스마트 인디고)
* **텍스트 & 보조 컬러**: `#F8FAFC` (화이트 실버), `#94A3B8` (슬레이트 그레이)
* **그림자 (Shadow)**: `box-shadow: 0 20px 40px rgba(0, 0, 0, 0.35);`

### 4.2. 웹 대시보드 페이지 구성 및 레이아웃

대시보드는 크게 **"오늘 하루"**, **"홈 제어"**, **"디버깅 로그"** 탭(또는 섹션)으로 구성되며, 화면 우측 하단에 **"간편 AI 챗봇"**이 상시 위치합니다.

#### 1️⃣ 오늘 하루 (Today's Summary)
1. **모드 설정 (Mode Panel)**:
   * **수면 모드**: 버튼 클릭 시 `'S'`를 전송하여 마이크 감지를 활성화합니다. 수면 모드가 ON되면 실시간 수면 질 분석 차트가 활성화됩니다.
   * **기상 모드**: 버튼 클릭 시 `'Q'`를 전송하여 수면 모드를 끕니다. 웹 대시보드에서 기상 알람 시간을 설정할 수 있으며, 알람이 발생했을 때 ESP32에서 비프음 경보가 울리면 웹의 "알람 끄기" 버튼(전송 명령 `'A'`)으로 부저 소리를 끌 수 있습니다.
   * **집중 모드**: 클릭 시 웹 브라우저에서 편안한 백그라운드 백색소음(ASMR 오디오 - 빗소리, 시냇물 소리 등)이 재생되며, 집중 집중을 돕는 타이머 및 알람 설정 기능이 활성화됩니다.
2. **오늘 일정 불러오기**:
   * 로컬에서 관리할 수 있는 심플한 Todo/일정 리스트 카드. 일정을 추가 및 삭제할 수 있어 오늘 하루 계획을 모니터링합니다.
3. **지역 날씨 불러오기**:
   * API를 사용하여 해당 지역(서울 등)의 실시간 날씨를 대시보드에 표시하고, 버튼 클릭 시 ESP32의 OLED 1 디스플레이에도 날씨 정보 및 아이콘이 동기화 표시되도록 지원합니다.
4. **수면 질 체크 (Snore Chart)**:
   * 수면 중 ESP32로부터 전송받은 실시간 마이크 레벨(`Mic Level: <value>`) 데이터를 누적하여 꺾은선 그래프(Chart.js 라이브러리 활용)로 수면 중 소음 상태를 비주얼하게 보여줍니다.

#### 2️⃣ 홈 제어 (Home Control)
1. **전등 제어 (RGB LED Switch)**:
   * 토글 스위치를 통해 스마트홈 전등을 켜고 끌 수 있습니다. (명령어 `'7'` / `'8'` 전송)
2. **블라인드 모터 제어 (SG90 Motor Slider)**:
   * 스마트 창문의 블라인드 각도를 조절할 수 있는 슬라이더 바(0° ~ 180°)를 제공하여, 실시간으로 `'M90'`, `'M180'`과 같이 모터 위치 조절 명령을 ESP32로 전송합니다.

#### 3️⃣ 간편 AI 챗봇 (AI Chatbot Assistant)
* 화면 우측 하단에 고정된 둥근 로봇 모양 플로팅 버튼.
* 클릭 시 대화형 챗봇 모달/슬라이드 창이 열립니다.
* 사용자가 "불 꺼줘", "수면모드 켜줘", "날씨 어때?" 등의 자연어로 입력하면 챗봇이 친절하게 답변하고, 이에 상응하는 BLE 제어 명령어(`'8'`, `'S'`, `'1'` 등)를 ESP32 기기로 자동 전송하여 음성 명령을 대체하는 스마트 제어를 지원합니다.

---

## 🚀 5. 구현 체크리스트 & 개발 가이드
* Web Bluetooth NUS 연결을 지원하는 `index.html` 단일 파일 웹앱을 구축합니다.
* 마이크 센서 값의 시각화를 위해 Chart.js 라이브러리를 CDN으로 로드하여 사용합니다.
* 백색소음 재생은 HTML5 `<audio>` 태그 또는 Web Audio API를 활용하여 브라우저에서 직접 재생을 처리합니다.
